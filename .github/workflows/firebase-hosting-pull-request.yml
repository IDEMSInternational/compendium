# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
"on": pull_request
jobs:
  build_and_preview:
    if: contains(github.event.pull_request.labels.*.name, 'Test - preview')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Populate environment config
        env:
          SUPABASE_CONFIG_TS: ${{ secrets.SUPABASE_CONFIG_TS }}
        run: echo $SUPABASE_CONFIG_TS > src/environments/supabaseConfig.ts
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_STAFF_DATABASE_4EFC8 }}"
          projectId: staff-database-4efc8
